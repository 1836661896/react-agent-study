---
description: 前端学习阶段计划——以 Vue 迁移视角学习 React，并逐步对接后端 API
alwaysApply: true
---

## 使用方式（给助手）

- 新对话/换设备：先读 `frontend/readme.md` 与本文件，再读 `react-learning-checklist.mdc`。
- 教学节奏：按阶段小步推进；每次只引入少量知识点，优先讲“与 Vue 不同的地方”和“为什么这样设计”。

---

## 项目推进计划（按难易：易 → 中 → 较难）

> 目标是尽快形成“最小闭环”，再逐步做结构化与可扩展 UI。

| 顺序 | 阶段 | 难易 | 目标与产出（前端） | 状态 |
|------|------|------|--------------------|------|
| 0 | 阶段 0：工程与运行 | 易 | 能跑起来、能构建；环境变量/代理就绪 | ✅ 已完成 |
| 1 | 阶段 1：React 基础与最小联调 | 易~中 | 理解函数组件/JSX/useState/useEffect；health + tasks；**Agent：`POST /agent/run`、`GET /agent/last-step`、`GET /agent/steps`（操作历史）** | ✅ 已完成 |
| 2 | 阶段 2：组件拆分与数据模型 | 中 | 拆 `AgentPanel`/`TaskSection`/`HealthHeader`（或等价）；统一 types；避免 App.tsx 过大 | ✅ 已完成 |
| 3 | 阶段 3：请求层与错误体验 | 中 | `http.ts` 错误分类、统一 toast/提示；Abort/超时；React Query 错误展示策略 | 🔄 **当前建议** |
| 4 | 阶段 4：可扩展 UI（面向 Agent） | 较难 | 任务详情/步骤时间线/日志查看；为工具调用结果展示留结构 | ⏳ 待开始 |

---

## 当前建议（下一步怎么学）

- **当前建议阶段**：**阶段 3**（阶段 2 已完成，见 `frontend/readme.md`）
- **本阶段目标**：
  - **`src/lib/http.ts`**：区分错误类型（网络失败、非 2xx、解析失败）；对外暴露统一错误信息或 `HttpError` 子类/字段，便于 UI 展示。
  - **全局/局部提示**：用 antd **`message`** 或 **`notification`** 统一错误文案风格；避免每个组件重复拼 `err.message`。
  - **React Query**：在任务列表、last-step、step-list 等 **`useQuery`** 上处理 **`isError` / `error`**（Card 内错误条、**重试**按钮）；可选 **`staleTime`**、**`refetchOnWindowFocus`**。
  - 进阶可选：**AbortController**、请求超时、Mutation 取消。
- **本阶段结束标准**（满足即可进入阶段 4）：
  - 主要请求均有可感知的错误态（非静默失败）；用户能区分「网络挂了」与「后端业务 code≠0」。
  - 能口述：React Query 的 `error` 与 `HttpError.body` 如何配合展示。

