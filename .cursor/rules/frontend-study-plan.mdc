---
description: 前端学习阶段计划——以 Vue 迁移视角学习 React，并逐步对接后端 API
alwaysApply: true
---

## 使用方式（给助手）

- 新对话/换设备：先读 `frontend/readme.md` 与本文件，再读 `react-learning-checklist.mdc`。
- 教学节奏：按阶段小步推进；每次只引入少量知识点，优先讲“与 Vue 不同的地方”和“为什么这样设计”。

---

## 项目推进计划（按难易：易 → 中 → 较难）

> 目标是尽快形成“最小闭环”，再逐步做结构化与可扩展 UI。

| 顺序 | 阶段 | 难易 | 目标与产出（前端） | 状态 |
|------|------|------|--------------------|------|
| 0 | 阶段 0：工程与运行 | 易 | 能跑起来、能构建；环境变量/代理就绪 | ✅ 已完成 |
| 1 | 阶段 1：React 基础与最小联调 | 易~中 | 理解函数组件/JSX/useState/useEffect；完成 health + tasks（查询/添加/删除）最小闭环 | 🔄 进行中 |
| 2 | 阶段 2：组件拆分与数据模型 | 中 | 拆 `TaskList`/`TaskForm`/`HealthBadge`；统一 types；避免 App.tsx 过大 | ⏳ 待开始 |
| 3 | 阶段 3：请求层与错误体验 | 中 | `http.ts` 错误分类、统一 toast/提示；Abort/超时；React Query 错误展示策略 | ⏳ 待开始 |
| 4 | 阶段 4：可扩展 UI（面向 Agent） | 较难 | 任务详情/步骤时间线/日志查看；为工具调用结果展示留结构 | ⏳ 待开始 |

---

## 当前建议（下一步怎么学）

- **当前建议阶段**：阶段 1
- **本阶段目标**：
  - 能用“Vue 对照”解释：`useQuery`/`useMutation`/`invalidateQueries`、受控输入、JSX 条件渲染与列表渲染
  - 能独立读懂 `App.tsx` 中的最小闭环
- **本阶段结束标准**（满足即可进入阶段 2）：
  - 知道 `queryKey` 是缓存 key，不是防抖
  - 能解释 `useEffect(..., [])` 相当于 `onMounted`
  - 能解释 `value + onChange` 相当于 `v-model`（受控组件）
  - 能说清“mutation 成功后为什么 invalidate queries”

