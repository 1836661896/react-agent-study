---
description: import 路径规范（相对 / @ 别名）
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# 引用路径

## 规则

| 场景 | 写法 |
|------|------|
| 同级目录 | `./foo` |
| 跨目录（pages → utils、api、types） | `@/utils/...`、`@/api/...`、`@/types/...` |
| 页面子组件访问本页类型等 | `../types` 或同目录 `./Xxx.types`（**仅一层** `../`） |
| 更深跳转 | 一律改为 `@/`，禁止 `../../..` |

## 示例

```tsx
// ✅ pages/chat/ChatThreadPanel/index.tsx
import { getConversationMessages } from '@/api/conversations'
import type { ChatMessage } from '@/types/conversations'
import { formatDisplayDateTime } from '@/utils/datetime'

// ❌
import { getConversationMessages } from '../../../api/conversations'
```

## 其他

- 类型使用 `import type { ... }`。
- import 顺序建议：builtin → external → `@/` → relative（Biome `organizeImports` 已开启）。
- 可复用逻辑从 `pages/` **下沉**到 `components/`、`utils/`、`api/`，避免页面间互相引用内部文件。
