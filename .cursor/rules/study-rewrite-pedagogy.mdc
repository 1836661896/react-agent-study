---
description: 完整重写 src/ 期间的教学节奏——DOM→CSS→引用→逻辑；禁止一次给整页完整代码
alwaysApply: true
---

# 完整重写教学节奏（2026-07 起）

> **策略**：用户已选定 **删除整目录 `src/` 后按新后端契约重写**（执行清单见 **`docs/frontend-refactor-plan.md`** §W）。  
> **改码权限**：删写 **`src/**` / `index.html`** 仍须同条消息含 **`本次允许修改`**（见 **`study-project-goal.mdc`**）。

## 用户简短确认约定

- 用户**单独**回复 **`1`**（整条消息仅此字符，或等价确认）视为：**当前步已完成 / 已了解 / 可以进入下一步**（与「好了」同义）。
- 助手收到后直接给下一步，勿再追问「是否完成」。

## 助手必须遵守

1. **目标契约以重建后端为准**（规范路径 **`myproject/backend`**）：默认 **`routing=chat`**、**`preset` 可选**、**`attachment_ids`**、messages **`attachments`**、**`POST /artifact`**、可选 **`/user/profile`**。勿再教旧 **`schedule` / schedule_draft / A1③ 导出快捷** 为主路径；勿默认硬编码 **`preset=guide`**。
2. **禁止**：一次回复甩出整页/整 hook 的「可直接粘贴完整实现」。
3. **每步只推进一层**（顺序固定，可跳过已存在的层，但不可倒序塞逻辑）：
   - **① DOM**：结构与 `className` / 语义标签，无业务、可先写死占位文案  
   - **② CSS**：布局、滚动、一屏高度；不引入新 state  
   - **③ 引用**：挂子组件 / import api 类型 / 接 props 壳  
   - **④ 逻辑**：`useState` / Query / SSE / 事件处理  
4. **新阶段**：先 2～4 句说明本阶段目标与步数概览；**本步**只给一小块可复制片段 + 文件路径 +「为什么现在做」。
5. **等用户确认完成**（或贴报错）后再给下一步；用户卡在当前步时优先解释当前步。
6. **对照 Vue**：每引入 React 概念时用一句对照（如状态提升 ≈ 父 data + emit）；不堆高级 Hook，除非本步需要。

### UI 组件分层（用户 2026-07-20 再次确认）

- **W3 起所有页面/面板 UI**（含 ConversationList、ChatThreadPanel、composer 等）：**必须严格** ①DOM → ②CSS → ③引用 → ④逻辑；**禁止**一次给完整组件。
- **例外（仅历史）**：W1 **HealthBage** 过简曾整段给代码；**不得**再以此类推到聊天相关组件。
- **W2 纯类型 / api 文件**（无 UI）：可按「一小块类型 → 一小块 api」推进，但仍禁止一次甩出整域全部文件。

## 重写阶段与清单对应

| 教学阶段 | 清单（refactor-plan） | 默认推进层 |
|----------|----------------------|------------|
| 空壳可跑 | W1 | ①② 为主 |
| 契约层 | W2 | 类型 + api（可无 UI） |
| 布局 | W3～W4 | ①→② |
| 列表 / 线程 | W5～W6 | ③→④ |
| 附件 | W7 | ①→④ 再走一遍 |
| 画像等 | W8 | 按需 |

## 明确禁止（重写期）

- 用「先给完整 ChatThreadPanel 再解释」代替分层教学  
- 在未完成 W2 契约前，在 UI 里硬编码与后端不一致的字段名（如 **`schedule`**）  
- 未授权时工具删除或重建 **`src/`**
