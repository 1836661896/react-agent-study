---
description: 前端项目规则——配合 Python 后端的高级 agent 系统，使用 React 实现 Web 界面
alwaysApply: true
---

## 前端项目定位与总体目标

- 本前端项目位于 `myproject/frontend`，主要职责是：
  - 作为**Python 后端 + Agent 系统**的 Web 界面层；
  - 为“高级自动化 + 视觉识别 + Agent 调度”的整体系统提供：任务创建、状态展示、日志查看、手动干预等交互能力。
- 前端需要支持的长期目标包括：
  - 以**任务 / 流程**为核心的 UI（例如任务列表、任务详情、执行步骤时间线、错误信息展示）。
  - 能够与后端的 Agent 模块进行交互：发送自然语言指令、查看工具调用结果、回放执行历史。
  - 为后续扩展（如屏幕识别结果可视化、自动化脚本配置界面）预留合理的信息结构和布局。

## 用户技术栈与教学风格

- 用户背景：
  - 具备多年 **前端 Vue 经验**，对 JavaScript 语法和工程化有较强理解；
  - **尚未使用过 React**，希望在本项目中系统性学习和实践；
  - 后端部分采用 Python（通过本仓库的 backend 项目逐步学习与实现）。
- 助手在前端相关对话中的教学要求：
  - 尽量用“**从 Vue 迁移到 React**”的视角解释概念，例如：
    - 组件写法（选项式 API vs 函数组件 + Hooks）；
    - 状态管理（Vue 的响应式 vs React 的 useState/useReducer）；
    - 模板语法（`v-if` / `v-for`）与 JSX 的对应关系。
  - 避免一次性抛出过多 React 高级特性，优先掌握：
    - 函数组件；
    - `useState`、`useEffect` 等基础 Hook；
    - 组件拆分与 props 传递；
    - 与后端 API 的请求交互。

## 当前后端进度与联调约定（随 backend 进度更新）

- **后端项目位置**：`myproject/backend`；进度以 **backend/readme.md** 为准。
- **统一响应信封**：业务接口普遍返回 **`{ code, data, msg }`**（与前端 `HttpError` / 页面判断 `res.code === 0` 对齐）；具体 `data` 形状见各路由。
- **当前后端 API（与 `backend/src/api.py` 对齐，随项目演进更新）**：
  - **GET /health**：健康检查。
  - **GET /tasks**、**POST /tasks**、**DELETE /tasks/{task_id}**：任务列表与增删；任务持久化 `tasks.json`。
  - **POST /agent/run**：请求体 `{ "text": string }`，执行 Agent 工具链。
  - **GET /agent/last-step**：最近一次 `Step`（无记录时按后端约定返回 `code`/`msg`）。
  - **GET /agent/steps?limit=**：操作历史（最近若干条 `Step`，供前端「操作历史」展示）。
- **联调注意**：后端进程重启后内存中的步骤历史会清空；调试历史接口前需先 **POST /agent/run** 产生步骤。
- **启动后端**：在 `backend` 目录执行 `uvicorn src.api:app --reload`，默认 `http://127.0.0.1:8000`；前端需后端开启 **CORS**。
- **当前前端学习阶段（与 `frontend/readme.md` 同步）**：阶段 1～2 已完成（API 联调、组件拆分）；**建议下一步：阶段 3**（`http` 错误分类、React Query 错误态与统一提示）。

## 前后端整体目标对齐

- 后端（Python）与前端（本项目）的长期共同目标：
  - 搭建一个可扩展的 agent 系统，最终能够：
    - 接收自然语言任务描述；
    - 通过后端 agent 调用多种工具（包括自动化脚本、屏幕识别模块等）；
    - 由前端以清晰的 UI 形式展示任务进度、结果和中间状态。
- 在设计前端结构时，助手应：
  - 尽量采用有利于**后续扩展“任务类型、工具种类、可视化组件”**的组件划分和数据结构；
  - 在不增加过多复杂度的前提下，为未来的：
    - 自动化任务配置界面；
    - 屏幕/游戏画面识别结果标注展示；
    - 历史任务回放视图；
    留出合理的抽象空间（例如使用统一的任务/步骤数据模型）。

## 文件修改与教学方式（前端部分）

### 规则优先级（与用户全局指令冲突时）

- **默认**：本仓库 **`frontend/src/**`**（及本仓库内其他前端业务源码，不含 `.cursor/rules/`）以**本条「引导用户亲手改」**为准。
- **与常见用户规则的关系**：若全局规则写有「直接执行、不要只给命令」等，在本仓库前端源码上**不优先于**本条；助手应**口述步骤 + 给出可复制片段 + 标清文件与位置**，由用户粘贴保存，而不是默认直接改文件。
- **例外（允许助手直接改 `src/`）**：仅当用户在本轮对话中**明确**提出，例如：「你直接改」「帮我改好」「可以代写/贴补丁」「Apply 到项目里」等。
- **`.cursor/rules/`**：为降低来回提醒成本，助手**可直接**增删改规则文件（含本文件），无需用户逐句授权。

- 除 `.cursor/rules/` 下的规则文件外，助手**不直接修改前端代码文件**，而是：
  - 详细说明：在哪个文件、哪个位置需要添加/修改什么代码；
  - 用中文解释每一段关键代码的作用，特别是与 Vue 思路不同的部分；
  - 引导用户亲自完成编辑，助手负责审核与讲解。
- 在引导用户编写 React 代码时：
  - 先给出当前小阶段的**目标和 2～4 个步骤概览**；
  - 每次只提供一小段可操作的代码或命令，等待用户执行并反馈后再继续；
  - 优先解释“为什么要这样设计组件/状态结构”，而不仅是“怎么写”。

