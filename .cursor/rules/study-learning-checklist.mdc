---
description: React 知识点与后端联调勾选——完整重写 W 阶段对齐重建后端
alwaysApply: true
---

> 说明：这里的「状态」用于控制学习节奏。每次学习结束，把本次涉及到的知识点从「未开始」改为「进行中/已完成」。

## 状态标记

- ✅ 已完成：能解释 + 能在当前项目里指出对应代码
- 🔄 进行中：讲过/用过，但还需要更多练习巩固
- ⏳ 未开始：还没引入
- 🗄 归档：旧实现曾掌握；完整重写后须在新代码中重新勾选

---

## React 专属知识点（重点）

### 1) 渲染模型

- 🗄 **组件渲染 = 执行函数**（旧 `src` 曾完成；重写 W1 起再练）
- ⏳ **不要在 render 里做副作用**
- ⏳ **闭包与函数式更新 / Abort**

### 2) Hooks 基础

- 🗄 **useState** / **useEffect**（旧代码曾用；W5～W6 重练）
- ⏳ **useMemo/useCallback**（有明确性能需求再引入）

### 3) JSX

- 🗄 条件 / 列表 / 事件 / 受控输入（旧代码）
- ⏳ **Props 与状态提升**（W5～W6：列表选中、preset=`guide`）

### 4) React Query

- 🗄 **useQuery / useMutation / invalidateQueries**（旧代码）
- ⏳ 重写后重新勾选；**isError UI**、**staleTime** 仍可后置

### 5) TypeScript

- ⏳ **对齐重建契约**：`AgentPreset = "guide"`、`attachment_ids`、`attachments`、`updated_at`
- ⏳ 类型收敛（少用 `any`）

---

### 6) 后端联调（重建 backend）

> 权威：**`myproject/backend/readme.md`**、**`docs/chat-stream-api.md`**、**`docs/conversations-api.md`**、**`docs/artifacts-api.md`**、**`docs/agent-presets.md`**。

- 🗄 **旧 A1**：`preset=schedule`、行程助手文案 — **不再作为主路径**
- ⏳ **GET /health**（W1：差 request + HealthBage）
- ⏳ **会话 list / create / delete / messages**（含 **`attachments`**）
- ⏳ **POST /chat/stream**（默认 **`chat`**；**`preset=guide`**；**`attachment_ids`**；SSE **`delta` / `tool_*` / `error` / `done`**）
- ⏳ **POST /artifact** 上传 + **GET /artifact/{id}** 下载
- ⏳ **GET/PUT /user/profile**（W8）
- ⏳ 流式 **Abort** UI（W8；后端 disconnect 未做，前端可先断连接）

**重写进度**：**W0 ✅**；**W1 🔄**（壳/路由/Provider 已有）。清单：**`docs/frontend-refactor-plan.md`** §**W**；换设备见 **`docs/study-progress.md`**。
