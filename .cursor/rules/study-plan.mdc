---
description: 前端学习阶段计划——2026-07 起按 W 阶段完整重写 src/，对齐重建后端
alwaysApply: true
---

## 使用方式（给助手）

- 新对话/换设备：先读本仓库根目录 **`readme.md`** + **`docs/documentation-index.md`**，再读本文件、**`study-rewrite-pedagogy.mdc`** 与 **`study-learning-checklist.mdc`**。
- **路径约定**：**`myproject/frontend`** / **`myproject/backend`** 为文档中的约定路径；不在此记录本机实际克隆目录。
- **重写教学节奏**：见 **`study-rewrite-pedagogy.mdc`**（DOM → CSS → 引用 → 逻辑；禁止一次给整页完整代码）。

---

## 项目推进计划（2026-07：完整重写）

> **选定策略（用户 2026-07-20）**：**删除整个 `src/` 后按重建后端契约重写**。执行勾选见 **`docs/frontend-refactor-plan.md`** §**W**。  
> **目标契约**：会话 + SSE；默认 **`routing=chat`**；**`preset` 可选**（日常可不传；导游才 `guide`）；上传 **`POST /artifact`** + **`attachment_ids`**；历史 **`attachments`**；可选画像 **`/user/profile`**。

| 顺序 | 阶段 | 难易 | 目标与产出 | 状态 |
|---|---|---|-----|---|
| — | 历史 R0～R4 / 旧 A1（`schedule`） | — | 旧实现仅作对照；重写后不再作为主路径 | 🗄 归档 |
| W0 | 快照 / 迁移 | 易 | 用户已将 `src/` 迁至 **`backup/src/`** | ✅ |
| W1 | 空壳可跑 | 易 | `main` / `App` / 路由 / Health / 全局样式 | ✅ |
| W2 | 类型与 API | 中 | conversations / chatStream / artifacts；对齐新字段 | ✅ |
| W3～W4 | 布局 DOM + CSS | 中 | `/chat` 双栏空壳 + 一屏高度与滚动 | ✅ |
| W5 | 会话列表 | 中 | list / create / delete | ✅ |
| W6 | 线程 + SSE | 较难 | messages + 流式；**`guide`** + **`chat`/`auto`**；tool 抽屉 | ✅ |
| W7 | 附件 | 较难 | 上传、粘贴文件、发送挂载、历史预览/下载 | ✅ |
| W8 | 可选增强 | 中 | 画像 UI、Abort、能力按钮 | ⏳（画像暂缓；Abort 待做） |
| W8′ | auto + 身份字典 + 展示 | 中 | 固定 auto、`/dict/presets`、antd、Enter 发送 | ✅ |

---

## 当前建议（下一步怎么学）

1. **换设备**：先读 **`docs/study-progress.md`**「换设备继续」。
2. **立刻**：**R4b.2** 附件解析联调（先读 backend 契约，再决定前端是否要改）。
3. 确认约定：单独回复 **`1`**。
4. 权威后端：**`myproject/backend/readme.md`**、**`agent-presets.md`**（**`/dict/presets`**）；旧代码只读 **`backup/src/`**。

## 与后端近期共识

- **身份**：列表 **`GET /dict/presets`**；发送可选 **`preset`**（值为 `value`）；「普通」不传；旧 **`schedule`** **不恢复**；废 **`/agent/presets`**。
- **routing**：前端日常**显式** **`auto`**；后端省略字段时默认仍是 **`chat`**；**`plan`** 占位。
- **附件**：先 **`POST /artifact`**，再 Chat 带 **`attachment_ids`**；messages 回 **`attachments`**。前端可粘贴任意文件；**暂不**处理文字+文件同贴。解析见 **R4b.2**。
