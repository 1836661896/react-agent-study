---
description: 规范适用边界、共享/私有划分、无用代码及时清理
alwaysApply: true
---

# 务实结构与清理

规范服务于可维护性，**不是**为了目录整齐。改代码前先判断页面/模块复杂度，再决定是否套用完整页面骨架。

## 规范适用边界（不盲目贴合）

| 页面类型 | 典型特征 | 做法 |
|----------|----------|------|
| **薄页面** | 仅组合子组件、几乎无本地状态 | `index.tsx` 即可；不必强行 `type.ts`、`hooks/` |
| **标准数据页** | 列表/表单 + Query + 若干事件 | 必要时拆 hook；有页面级类型再建 **`type.ts`** |
| **复杂页** | 多段 SSE、乐观更新、单文件 >~300 行且职责可切分 | 拆 hooks / 子组件；**不为达标而拆** |

### 禁止

- 为两个结构相近的页面抽「万能 Chat 壳」，若 props 需塞事件类型、模式枚举、工具条配置 → 耦合过高，保留各页实现
- 无页面级类型时创建空 `type.ts`
- 逻辑仍可读时，仅为压到 300 行而机械拆文件
- 批量改 import 路径（`@/` ↔ `./`）若无功能收益

### 允许

- 单文件略超 ~300 行，若逻辑内聚、阅读顺畅（如流式聊天主面板）
- 子面板继续用 `index.scss`，与 `coding-styling.mdc` 存量惯例一致

页面子目录基础文件：`index.tsx`、`index.scss`；有类型时加 **`type.ts`**（没有则不建）。

## 共享 vs 页面私有

### 放入 `src/components/` 的条件

1. **≥2 个页面**会复用（非「将来可能」）
2. 通过 props 注入数据与回调，**不** import `@/pages/*` 内部 hook
3. 不写死某一页的 mutation 组合或路由参数

### 留在 `pages/{feature}/` 内

- 只服务聊天页的会话列表列、消息行、composer、SSE 状态机
- 强依赖该页 hook 返回类型整块下传 → 页面私有

## 及时清理（重构/删功能后同步）

完成重命名或舍弃功能后，**同一任务内**检查：

| 类别 | 示例 |
|------|------|
| 死代码 | 未引用的组件、api 函数、类型 |
| 空目录 | 删文件后移除空文件夹 |
| 过时文档 | `frontend-refactor-plan` 仍写已删除 API |
| 契约残留 | `frontend-backend-contract` 仍列已下线路由 |

### 清理顺序

1. `rg` 确认零引用 → 2. 删实现 → 3. 更新契约/refactor-plan → 4. `tsc --noEmit` / `npm run lint`

## 与相邻规则

- 目录与分层 → `coding-architecture.mdc`
- 何时拆 hook → `coding-react-components.mdc`（本规则优先：简单页不强行拆）
