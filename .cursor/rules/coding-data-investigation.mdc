---
description: 数据/接口异常时不盲改 — 先确认接口与返回值
alwaysApply: true
---

# 数据与接口问题：先确认，再改代码

涉及 **接口返回值、字段含义、枚举、分页结构、SSE 事件形态** 等问题时，**禁止**在未确认事实前改实现来「碰运气」。

## 必须做的

1. **先描述现象**：页面表现、期望行为、当前代码依据。
2. **明确向用户索取信息**，写清：
   - **哪个接口**（路径 / `api/*` 函数名）
   - **需要什么**：完整响应 JSON、SSE 事件片段、关键字段示例、出错时的 `code`/报错文案、请求参数
   - **哪条分支**：列表/流式发送/工具回调、何种 `routing` 或 `preset` 下复现
3. **给出 1～2 个可验证假设**，说明缺哪段数据就无法判断；等用户补充或确认后再改。

## 禁止做的

- 凭 TypeScript 类型、字段命名、或「看起来合理」**推断**后端语义并直接改代码
- 用 `??`、`as any`、兜底默认值、静默吞错等方式 **掩盖** 数据结构不明的问题
- 在未确认契约前，连续多轮「试一种映射 / 试一种枚举」式修改

## 权威来源

| 情况 | 做法 |
|------|------|
| 前端与 backend 文档不一致 | 并排指出差异；**优先请用户提供实际响应或确认 backend 最新行为** |
| `docs/frontend-backend-contract.md` 与代码冲突 | 指出冲突，请用户确认以契约还是现网为准 |
| 纯前端 bug（拼写、未接 hook、明显遗漏）且与接口语义无关 | 可直接修 |

## 向用户提问示例

> 消息列表时间显示异常。请提供 **`GET /conversation/{id}/messages`** 在问题会话下的响应片段，尤其是 `created_at` 字段格式。  
> 若方便，请说明是否仅 SSE 新消息有问题。确认前我不会改 `formatDisplayDateTime` 或列表渲染逻辑。

## 可以改代码的前提

- 用户已提供可核对的响应或确认字段含义；或
- DevTools / 本地日志已有明确证据；或
- 纯前端非契约问题且与接口语义无关
