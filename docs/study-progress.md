# 学习进度与文档提交约定

> **学习节奏与「下一步」**；**不替代**根目录 **`readme.md`** 中的功能模块表。

---

## 最近一次学习（2026-07-24 · 附件主路径验收）

- **本仓**：修 **`downloadArtifact`**（JSON 误判）；附件上传/回复/下载/解析与跨仓联调已收口。  
- **下一步（前端）**：等 backend **失败可见 / Abort** 协议后再改停止按钮等。  
- 详见 backend **`readme.md` §7**。

---

## 换设备继续（下一步）

1. 拉最新 **`main`**。  
2. 不插队语音；从 **失败可见 / Abort** 跟 backend。  
3. 附件主路径无需再测（除非改下载/解析契约）。

---

## 提交前约定

1. 有代码时建议 **`npm run lint`**。
2. 更新 **`readme.md`** 功能表、**`changelog.md`**、勾选 **`frontend-refactor-plan.md`**。
3. 教学默认不工具改 **`src/**`**，除非消息含 **`本次允许修改`**。
